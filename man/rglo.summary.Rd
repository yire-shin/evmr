% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rglo.summary.R
\name{rglo.summary}
\alias{rglo.summary}
\title{Summary of rglo.fit}
\usage{
rglo.summary(
  data,
  r = NULL,
  ydat = NULL,
  mul = NULL,
  sigl = NULL,
  shl = NULL,
  mulink = identity,
  siglink = identity,
  shlink = identity,
  num_inits = 100,
  muinit = NULL,
  siginit = NULL,
  shinit = NULL,
  show = FALSE,
  method = "Nelder-Mead",
  maxit = 10000,
  ...
)
}
\arguments{
\item{data}{A numeric vector, matrix, or data frame containing the
r-largest order statistics. Each row should contain decreasing order
statistics for one block or time period.}

\item{r}{Optional integer specifying the maximum number of order
statistics to summarize. If \code{NULL}, all available columns are used.}

\item{ydat}{A matrix of covariates for generalized linear modelling of the
parameters, or \code{NULL} for stationary fitting.}

\item{mul, sigl, shl}{Numeric vectors of integers giving the columns of
\code{ydat} to be used for the location, scale, and shape parameters,
respectively.}

\item{mulink, siglink, shlink}{Inverse link functions for the location,
scale, and shape parameters, respectively.}

\item{num_inits}{Number of initial parameter sets used in optimization.}

\item{muinit, siginit, shinit}{Optional initial values for the location,
scale, and shape parameters.}

\item{show}{Logical; if \code{TRUE}, print details from model fitting.}

\item{method}{Optimization method passed to \code{\link{optim}}.}

\item{maxit}{Maximum number of iterations for optimization.}

\item{\dots}{Additional arguments passed to \code{rglo.fit()}.}
}
\value{
A data frame containing:
\itemize{
\item \code{r}: number of order statistics used
\item \code{nllh}: negative log-likelihood
\item \code{mu}, \code{sigma}, \code{xi}: parameter estimates
\item \code{mu.se}, \code{sigma.se}, \code{xi.se}: standard errors
\item return levels and their standard errors for 20-, 50-, 100-, and 200-year periods
}
}
\description{
Summarize the fitted generalized logistic distribution models for
r-largest order statistics over \eqn{r = 1, \dots, R}. For each value
of \code{r}, the function fits the model using \code{rglo.fit()} and
computes return levels using \code{rglo.rl()}.
}
\examples{
\dontrun{
data(bangkok)
rglo.summary(bangkok)
}
}
